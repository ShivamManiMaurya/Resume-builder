import React from "react";

const Border = ({ children }) => {
    return (
        <div>
            ---------------------------------------------------------------------
            {children}
            ---------------------------------------------------------------------
        </div>
    );
};

export default Border;
